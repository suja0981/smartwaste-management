# ============================================================
# Smart Waste Management — Backend Dependencies
# ============================================================

# ── Core API ─────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0       # [standard] includes websockets + httptools
sqlalchemy==2.0.31
alembic==1.18.4                  # DB schema migrations
python-dotenv==1.0.0
pydantic==2.8.2
pydantic-settings==2.2.0
requests==2.31.0

# ── DB Drivers ───────────────────────────────────────────────
psycopg2-binary==2.9.9           # PostgreSQL driver
# psycopg==3.1.12                # Faster async driver — optional

# ── Authentication ───────────────────────────────────────────
# python-jose handles all JWT encode/decode — PyJWT is not used
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── Firebase Admin SDK (Auth + FCM) ──────────────────────────
firebase-admin==6.5.0

# ── WebSocket support (already bundled in uvicorn[standard]) ─
websockets>=12.0                 # explicit pin for clarity

# ── Phase 5: Report export ───────────────────────────────────
reportlab==4.2.0                 # PDF generation
openpyxl==3.1.2                  # Excel generation

# ── Utils ────────────────────────────────────────────────────
python-multipart==0.0.9          # multipart form data

# ── ML / numerics ────────────────────────────────────────────
numpy==1.26.4

# ── Testing ──────────────────────────────────────────────────
pytest==7.4.4
pytest-asyncio==0.23.2
httpx==0.27.0                    # async test client for FastAPI